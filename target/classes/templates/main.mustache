
<html>

<body>
    <div>
        <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <input type="submit" value="Sign Out"/>
        </form>
    </div>
    <span><a href="/user">Users</a> </span>

    <div>
        <form method="post"action="add">
            <input type="text" name="text" placeholder="Введите сообщение" />
            <input type="text" name="tag" placeholder="Тэг">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <button type="submit">Добавить</button>
        </form>
    </div>

    <div>Список сообщений</div>

    <form method="post" action="filterTag">
        <input type="text" name="filterTag">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <button type="submit">Найти</button>
    </form>

    <form method="post" action="filterAuthor">
        <input type="text" name="filterAuthor">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <button type="submit">Найти автора</button>
    </form>

    <form method="post" action="filterDate">
        <input type="text" name="filterDate">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <button type="submit">Введите дату формата yyyy.MM.dd </button>
    </form>

    <div>
        <form method="post" action="filterTagAuthor">
            <input type="text" name="authorName" placeholder="Имя автора" />
            <input type="text" name="tagName" placeholder="Тэг сообщения">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <button type="submit">Добавить</button>
        </form>
    </div>

    <style>
        table {
            border-collapse: collapse;
        }

        table, td, th {
            border: 1px solid #999;
            padding: 5px;
        }
    </style>

    <table>
        <tr>
            <th>Id</th>
            <th>Text</th>
            <th>Author name</th>
            <th>Date</th>
            <th>Time</th>
        </tr>

        {{#messages}}
            <tr>
                <td>{{id}}</td>
                <td>{{text}}</td>
                <td>{{authorName}}</td>
                <td>{{messageDate}}</td>
                <td>{{messageTime}}</td>
            </tr>
        {{/messages}}
    </table>
</body>
